<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.fireapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RestaurantProfileDisplay"></activity>
        <activity android:name=".RestaurantEditProfileActivity" />
        <activity android:name=".HelpRestaurant" />
        <activity android:name=".CustomerProfile" />
        <activity android:name=".SignUpAsCustomerActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".RestaurantProfile" />
        <activity android:name=".SignUpAsRestaurantActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>